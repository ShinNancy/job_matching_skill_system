# Core Scrapy
scrapy==2.11.2
scrapy-playwright==0.0.40

# MinIO client
minio==7.2.9

# PostgreSQL driver
psycopg2-binary==2.9.9

# Redis — incremental crawl cache + cross-run dedup
redis==5.0.4

# Item adapter (Scrapy helper)
itemadapter==0.9.0

# Async support
twisted==24.3.0
